# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust/Tauri
src-tauri/target/
**/*.rs.bk
# Cargo.lock wird bewusst versioniert: Dies ist eine Anwendung, kein
# Bibliotheks-Crate. Nur so sind Builds für Mitwirkende reproduzierbar.
# (Empfehlung des Cargo-Handbuchs, Abschnitt "Cargo.lock and Cargo.toml")

# Sensitive files (S4)
.env
.env.*
!.env.example
secrets.json
*.key
*.pem
*.p12
*.pfx

# Erzeugtes Updater-Manifest, gehoert ins Release, nicht ins Archiv
latest.json
